# Copie este arquivo para `.env` e preencha os valores locais.

# URL base da API. Vazia em dev: os caminhos ficam relativos e caem no proxy
# do Vite (vite.config.js), evitando CORS. Em produção (Vercel), aponte para
# a URL publicada do backend (Railway), sem barra no final.
VITE_API_URL=
